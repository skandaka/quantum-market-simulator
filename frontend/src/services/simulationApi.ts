// Re-export from the main API service for backward compatibility
export { simulationApi } from './api';

// Import and re-export as default
import { simulationApi } from './api';
export default simulationApi;
